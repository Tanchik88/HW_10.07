const getElementByIndex = (arr, index) => arr[index];

// Пример использования
const array = ['apple', 'banana', 'orange'];
const index = 1;
const result = getElementByIndex(array, index);
console.log(result); // 'banana'